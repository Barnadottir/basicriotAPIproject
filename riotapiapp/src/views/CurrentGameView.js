const CurrentGameView = (props) => {

    console.log("data in Current Game View: ",props.data);

    return (
        <div>
            Now we are here!
        </div>
    )
}

export default CurrentGameView;